---
apiVersion: v1
kind: Service
metadata:
  name: telegram-bridge
  namespace: flux-system
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: telegram-bridge
