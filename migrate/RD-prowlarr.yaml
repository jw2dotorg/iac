apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: restore-prowlarr
  namespace: media
spec:
  trigger:
    manual: restore-once
  kopia:
    repository: prowlarr-volsync-secret
    cacheStorageClassName: ceph-block
    capacity: 1Gi
    copyMethod: Snapshot
    moverSecurityContext:
      fsGroup: 568
      runAsGroup: 568
      runAsUser: 568
    sourceIdentity:
      sourceName: migrate-prowlarr
      sourceNamespace: media
    destinationPVC: prowlarr
    storageClassName: ceph-block
    accessModes:
      - ReadWriteOnce
    # Cleanup
    cleanupCachePVC: true
    cleanupTempPVC: true
    volumeSnapshotClassName: ceph-block
