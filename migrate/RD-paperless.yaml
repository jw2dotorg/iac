apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: restore-paperless
  namespace: default
spec:
  trigger:
    manual: restore-once
  kopia:
    repository: paperless-volsync-secret
    cacheStorageClassName: ceph-block
    cacheCapacity: 10Gi
    copyMethod: Snapshot
    moverSecurityContext:
      fsGroup: 568
      runAsGroup: 568
      runAsUser: 568
    sourceIdentity:
      sourceName: migrate-paperless
      sourceNamespace: default
    destinationPVC: paperless
    storageClassName: ceph-block
    accessModes:
      - ReadWriteOnce
    # Cleanup
    cleanupCachePVC: true
    cleanupTempPVC: true
    volumeSnapshotClassName: ceph-block
