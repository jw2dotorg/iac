apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: restore-karakeep
  namespace: default
spec:
  trigger:
    manual: restore-once
  kopia:
    repository: karakeep-volsync-secret
    cacheStorageClassName: ceph-block
    cacheCapacity: 1Gi
    copyMethod: Snapshot
    moverSecurityContext:
      fsGroup: 568
      runAsGroup: 568
      runAsUser: 568
    sourceIdentity:
      sourceName: migrate-karakeep
      sourceNamespace: default
    destinationPVC: karakeep
    storageClassName: ceph-block
    accessModes:
      - ReadWriteOnce
    # Cleanup
    cleanupCachePVC: true
    cleanupTempPVC: true
    volumeSnapshotClassName: ceph-block
