apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: restore-sonarr
  namespace: media
spec:
  trigger:
    manual: restore-once
  kopia:
    repository: sonarr-volsync-secret
    cacheStorageClassName: ceph-block
    cacheCapacity: 1Gi
    copyMethod: Snapshot
    moverSecurityContext:
      fsGroup: 568
      runAsGroup: 568
      runAsUser: 568
    sourceIdentity:
      sourceName: migrate-sonarr
      sourceNamespace: media
    destinationPVC: sonarr
    storageClassName: ceph-block
    accessModes:
      - ReadWriteOnce
    # Cleanup
    cleanupCachePVC: true
    cleanupTempPVC: true
    volumeSnapshotClassName: ceph-block
